//一元二次方程
/* 思路：
一元二次方程ax^2+bx+c=0的求解有下列几种情况：
1.当a=0时，该方程不是一元二次方程的解。
2.当a≠0时，该方程是一元二次方程。
（1）当b^2-4ac=0时，只有一个解；
（2）当b^2-4ac>0时，有两个不相同的解；
（3）当b^2-4ac<0时，该方程无解。*/
//求解时利用求根公式
#include <stdio.h>//头文件
#include <math.h>//调用sqrt 函数  求算术平方根
void main()//主函数
{
	float a, b, c, x1, x2, d;//定义变量
	printf("请输入a：");
	scanf_s("%f", &a);//输入x平方的系数 a的值
	printf("请输入b：");
	scanf_s("%f", &b);//输入x的系数 b的值
	printf("请输入c：");
	scanf_s("%f", &c);//输入常数项  c的值
	d = b * b - 4 * a * c;//求d 用于判断方程解的个数 和用于求根公式求解未知数的值
	if (d < 0)//条件 如果d小于0的话
	printf("方程没有实数解\n");//打印
	if (d == 0)//条件 如果d等于0的话
	{
		x1 = (-b) / (2 * a);//方程只有一个解 
		printf("x1=%f\n", x1);//输出这个方程的解
	}
	if (d > 0)//条件 如果d大于0的话
	{
		x1 = (-b - sqrt(d))/ (2 * a);//利用求根公式求x1
		x2 = (-b - sqrt(d)) / (2 * a);//利用求根公式求x2
		printf("x1=%f,x2=%f\n", x1, x2);//把x1，x2的值打印输出
	}
}/*注： 用float定义 是因为一元二次方程的解不一定都是整数*/
